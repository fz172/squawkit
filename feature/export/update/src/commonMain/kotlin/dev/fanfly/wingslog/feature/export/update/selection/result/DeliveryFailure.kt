package dev.fanfly.wingslog.feature.export.update.selection.result

/** Failed-delivery summary folded into the receipt card, so the success screen stays one card. */
internal data class DeliveryFailure(val title: String, val message: String)
