<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!--
    Declared here rather than relied on from the app manifest: this module is the one that calls
    loadAd(), so it should carry its own requirement. The manifest merger unions it into every
    host, and lint checks each module against what that module declares.
  -->
  <uses-permission android:name="android.permission.INTERNET" />

</manifest>
