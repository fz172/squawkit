// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: datalog/data_log.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { DataLogId, UserId } from "../id/ids";
import { Attachment } from "../thing/attachment";

export const protobufPackage = "";

export enum DataLogFormat {
  DATA_LOG_FORMAT_UNKNOWN = 0,
  DATA_LOG_FORMAT_GARMIN_G3X = 1,
  DATA_LOG_FORMAT_GARMIN_G1000 = 2,
  DATA_LOG_FORMAT_DYNON_SKYVIEW = 3,
  UNRECOGNIZED = -1,
}

export function dataLogFormatFromJSON(object: any): DataLogFormat {
  switch (object) {
    case 0:
    case "DATA_LOG_FORMAT_UNKNOWN":
      return DataLogFormat.DATA_LOG_FORMAT_UNKNOWN;
    case 1:
    case "DATA_LOG_FORMAT_GARMIN_G3X":
      return DataLogFormat.DATA_LOG_FORMAT_GARMIN_G3X;
    case 2:
    case "DATA_LOG_FORMAT_GARMIN_G1000":
      return DataLogFormat.DATA_LOG_FORMAT_GARMIN_G1000;
    case 3:
    case "DATA_LOG_FORMAT_DYNON_SKYVIEW":
      return DataLogFormat.DATA_LOG_FORMAT_DYNON_SKYVIEW;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DataLogFormat.UNRECOGNIZED;
  }
}

export function dataLogFormatToJSON(object: DataLogFormat): string {
  switch (object) {
    case DataLogFormat.DATA_LOG_FORMAT_UNKNOWN:
      return "DATA_LOG_FORMAT_UNKNOWN";
    case DataLogFormat.DATA_LOG_FORMAT_GARMIN_G3X:
      return "DATA_LOG_FORMAT_GARMIN_G3X";
    case DataLogFormat.DATA_LOG_FORMAT_GARMIN_G1000:
      return "DATA_LOG_FORMAT_GARMIN_G1000";
    case DataLogFormat.DATA_LOG_FORMAT_DYNON_SKYVIEW:
      return "DATA_LOG_FORMAT_DYNON_SKYVIEW";
    case DataLogFormat.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum DataLogSeriesKind {
  DATA_LOG_SERIES_KIND_UNKNOWN = 0,
  DATA_LOG_SERIES_KIND_NUMERIC = 1,
  DATA_LOG_SERIES_KIND_DISCRETE = 2,
  DATA_LOG_SERIES_KIND_TEXT = 3,
  DATA_LOG_SERIES_KIND_POSITION = 4,
  UNRECOGNIZED = -1,
}

export function dataLogSeriesKindFromJSON(object: any): DataLogSeriesKind {
  switch (object) {
    case 0:
    case "DATA_LOG_SERIES_KIND_UNKNOWN":
      return DataLogSeriesKind.DATA_LOG_SERIES_KIND_UNKNOWN;
    case 1:
    case "DATA_LOG_SERIES_KIND_NUMERIC":
      return DataLogSeriesKind.DATA_LOG_SERIES_KIND_NUMERIC;
    case 2:
    case "DATA_LOG_SERIES_KIND_DISCRETE":
      return DataLogSeriesKind.DATA_LOG_SERIES_KIND_DISCRETE;
    case 3:
    case "DATA_LOG_SERIES_KIND_TEXT":
      return DataLogSeriesKind.DATA_LOG_SERIES_KIND_TEXT;
    case 4:
    case "DATA_LOG_SERIES_KIND_POSITION":
      return DataLogSeriesKind.DATA_LOG_SERIES_KIND_POSITION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DataLogSeriesKind.UNRECOGNIZED;
  }
}

export function dataLogSeriesKindToJSON(object: DataLogSeriesKind): string {
  switch (object) {
    case DataLogSeriesKind.DATA_LOG_SERIES_KIND_UNKNOWN:
      return "DATA_LOG_SERIES_KIND_UNKNOWN";
    case DataLogSeriesKind.DATA_LOG_SERIES_KIND_NUMERIC:
      return "DATA_LOG_SERIES_KIND_NUMERIC";
    case DataLogSeriesKind.DATA_LOG_SERIES_KIND_DISCRETE:
      return "DATA_LOG_SERIES_KIND_DISCRETE";
    case DataLogSeriesKind.DATA_LOG_SERIES_KIND_TEXT:
      return "DATA_LOG_SERIES_KIND_TEXT";
    case DataLogSeriesKind.DATA_LOG_SERIES_KIND_POSITION:
      return "DATA_LOG_SERIES_KIND_POSITION";
    case DataLogSeriesKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** How `DataLog.raw_file`'s stored bytes wrap the original file. */
export enum DataLogEncoding {
  DATA_LOG_ENCODING_UNKNOWN = 0,
  DATA_LOG_ENCODING_NONE = 1,
  DATA_LOG_ENCODING_GZIP = 2,
  UNRECOGNIZED = -1,
}

export function dataLogEncodingFromJSON(object: any): DataLogEncoding {
  switch (object) {
    case 0:
    case "DATA_LOG_ENCODING_UNKNOWN":
      return DataLogEncoding.DATA_LOG_ENCODING_UNKNOWN;
    case 1:
    case "DATA_LOG_ENCODING_NONE":
      return DataLogEncoding.DATA_LOG_ENCODING_NONE;
    case 2:
    case "DATA_LOG_ENCODING_GZIP":
      return DataLogEncoding.DATA_LOG_ENCODING_GZIP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DataLogEncoding.UNRECOGNIZED;
  }
}

export function dataLogEncodingToJSON(object: DataLogEncoding): string {
  switch (object) {
    case DataLogEncoding.DATA_LOG_ENCODING_UNKNOWN:
      return "DATA_LOG_ENCODING_UNKNOWN";
    case DataLogEncoding.DATA_LOG_ENCODING_NONE:
      return "DATA_LOG_ENCODING_NONE";
    case DataLogEncoding.DATA_LOG_ENCODING_GZIP:
      return "DATA_LOG_ENCODING_GZIP";
    case DataLogEncoding.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** The recorder's own header, verbatim strings. */
export interface DataLogSource {
  /** "GDU 460" */
  product: string;
  /** "PFD1" */
  unit: string;
  softwareVersion: string;
  systemId: string;
  /** aircraft_ident on Garmin; compared with the Thing (PRD R11) */
  identity: string;
  airframeHours: string;
  engineHours: string;
}

/**
 * One column of the source file: the catalogue entry the list and sidebar render without touching
 * the bytes.
 */
export interface DataLogSeries {
  /** index in the source file */
  column: number;
  /** "Oil Press" */
  name: string;
  /** "E1 OilP" */
  shortName: string;
  /** "PSI", as recorded (PRD decision 6) */
  unit: string;
  kind: DataLogSeriesKind;
  /** "engine[1].oil_press" or "" (design §6.3) */
  canonicalId: string;
  min: number;
  max: number;
  /** non-empty cells */
  sampleCount: number;
}

export interface DataLog {
  /** boxed, never a bare string (design §4.4) */
  id: DataLogId | undefined;
  format: DataLogFormat;
  /** bumps when the parser changes what it emits */
  parserVersion: number;
  source: DataLogSource | undefined;
  start: Date | undefined;
  utcOffsetMinutes: number;
  durationSeconds: number;
  /** rows */
  sampleCount: number;
  /** median of 1/dt */
  sampleRateHz: number;
  series: DataLogSeries[];
  /**
   * The stored bytes, as an Attachment so every blob mechanism (reconciler, tombstone GC, server
   * GC, upload drivers, broker) applies unchanged. Type FILE, id = blob id; `sha256` and
   * `size_bytes` describe the STORED bytes, which is what the download driver verifies.
   */
  rawFile: Attachment | undefined;
  encoding: DataLogEncoding;
  /** of the original file, for duplicate detection (PRD R10) */
  rawSha256: string;
  rawSizeBytes: number;
  fileName: string;
  identityMismatch: boolean;
  /** false => "Ground run" (PRD R13) */
  airborne: boolean;
  /** from the filename where the format carries one (PRD R36) */
  startLocationIdent: string;
  /** for the V2 server lookup */
  endLatitude: number;
  endLongitude: number;
  /** written by the server in V2, empty until then */
  endLocationIdent: string;
  createdAt: Date | undefined;
  createdBy: UserId | undefined;
}

function createBaseDataLogSource(): DataLogSource {
  return { product: "", unit: "", softwareVersion: "", systemId: "", identity: "", airframeHours: "", engineHours: "" };
}

export const DataLogSource: MessageFns<DataLogSource> = {
  encode(message: DataLogSource, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.product !== "") {
      writer.uint32(10).string(message.product);
    }
    if (message.unit !== "") {
      writer.uint32(18).string(message.unit);
    }
    if (message.softwareVersion !== "") {
      writer.uint32(26).string(message.softwareVersion);
    }
    if (message.systemId !== "") {
      writer.uint32(34).string(message.systemId);
    }
    if (message.identity !== "") {
      writer.uint32(42).string(message.identity);
    }
    if (message.airframeHours !== "") {
      writer.uint32(50).string(message.airframeHours);
    }
    if (message.engineHours !== "") {
      writer.uint32(58).string(message.engineHours);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataLogSource {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataLogSource();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.product = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.unit = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.softwareVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.systemId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.identity = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.airframeHours = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.engineHours = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataLogSource {
    return {
      product: isSet(object.product) ? globalThis.String(object.product) : "",
      unit: isSet(object.unit) ? globalThis.String(object.unit) : "",
      softwareVersion: isSet(object.softwareVersion)
        ? globalThis.String(object.softwareVersion)
        : isSet(object.software_version)
        ? globalThis.String(object.software_version)
        : "",
      systemId: isSet(object.systemId)
        ? globalThis.String(object.systemId)
        : isSet(object.system_id)
        ? globalThis.String(object.system_id)
        : "",
      identity: isSet(object.identity) ? globalThis.String(object.identity) : "",
      airframeHours: isSet(object.airframeHours)
        ? globalThis.String(object.airframeHours)
        : isSet(object.airframe_hours)
        ? globalThis.String(object.airframe_hours)
        : "",
      engineHours: isSet(object.engineHours)
        ? globalThis.String(object.engineHours)
        : isSet(object.engine_hours)
        ? globalThis.String(object.engine_hours)
        : "",
    };
  },

  toJSON(message: DataLogSource): unknown {
    const obj: any = {};
    if (message.product !== "") {
      obj.product = message.product;
    }
    if (message.unit !== "") {
      obj.unit = message.unit;
    }
    if (message.softwareVersion !== "") {
      obj.softwareVersion = message.softwareVersion;
    }
    if (message.systemId !== "") {
      obj.systemId = message.systemId;
    }
    if (message.identity !== "") {
      obj.identity = message.identity;
    }
    if (message.airframeHours !== "") {
      obj.airframeHours = message.airframeHours;
    }
    if (message.engineHours !== "") {
      obj.engineHours = message.engineHours;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataLogSource>, I>>(base?: I): DataLogSource {
    return DataLogSource.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataLogSource>, I>>(object: I): DataLogSource {
    const message = createBaseDataLogSource();
    message.product = object.product ?? "";
    message.unit = object.unit ?? "";
    message.softwareVersion = object.softwareVersion ?? "";
    message.systemId = object.systemId ?? "";
    message.identity = object.identity ?? "";
    message.airframeHours = object.airframeHours ?? "";
    message.engineHours = object.engineHours ?? "";
    return message;
  },
};

function createBaseDataLogSeries(): DataLogSeries {
  return { column: 0, name: "", shortName: "", unit: "", kind: 0, canonicalId: "", min: 0, max: 0, sampleCount: 0 };
}

export const DataLogSeries: MessageFns<DataLogSeries> = {
  encode(message: DataLogSeries, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.column !== 0) {
      writer.uint32(8).int32(message.column);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.shortName !== "") {
      writer.uint32(26).string(message.shortName);
    }
    if (message.unit !== "") {
      writer.uint32(34).string(message.unit);
    }
    if (message.kind !== 0) {
      writer.uint32(40).int32(message.kind);
    }
    if (message.canonicalId !== "") {
      writer.uint32(50).string(message.canonicalId);
    }
    if (message.min !== 0) {
      writer.uint32(57).double(message.min);
    }
    if (message.max !== 0) {
      writer.uint32(65).double(message.max);
    }
    if (message.sampleCount !== 0) {
      writer.uint32(72).int32(message.sampleCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataLogSeries {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataLogSeries();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.column = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.shortName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.unit = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.canonicalId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 57) {
            break;
          }

          message.min = reader.double();
          continue;
        }
        case 8: {
          if (tag !== 65) {
            break;
          }

          message.max = reader.double();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.sampleCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataLogSeries {
    return {
      column: isSet(object.column) ? globalThis.Number(object.column) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      shortName: isSet(object.shortName)
        ? globalThis.String(object.shortName)
        : isSet(object.short_name)
        ? globalThis.String(object.short_name)
        : "",
      unit: isSet(object.unit) ? globalThis.String(object.unit) : "",
      kind: isSet(object.kind) ? dataLogSeriesKindFromJSON(object.kind) : 0,
      canonicalId: isSet(object.canonicalId)
        ? globalThis.String(object.canonicalId)
        : isSet(object.canonical_id)
        ? globalThis.String(object.canonical_id)
        : "",
      min: isSet(object.min) ? globalThis.Number(object.min) : 0,
      max: isSet(object.max) ? globalThis.Number(object.max) : 0,
      sampleCount: isSet(object.sampleCount)
        ? globalThis.Number(object.sampleCount)
        : isSet(object.sample_count)
        ? globalThis.Number(object.sample_count)
        : 0,
    };
  },

  toJSON(message: DataLogSeries): unknown {
    const obj: any = {};
    if (message.column !== 0) {
      obj.column = Math.round(message.column);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.shortName !== "") {
      obj.shortName = message.shortName;
    }
    if (message.unit !== "") {
      obj.unit = message.unit;
    }
    if (message.kind !== 0) {
      obj.kind = dataLogSeriesKindToJSON(message.kind);
    }
    if (message.canonicalId !== "") {
      obj.canonicalId = message.canonicalId;
    }
    if (message.min !== 0) {
      obj.min = message.min;
    }
    if (message.max !== 0) {
      obj.max = message.max;
    }
    if (message.sampleCount !== 0) {
      obj.sampleCount = Math.round(message.sampleCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataLogSeries>, I>>(base?: I): DataLogSeries {
    return DataLogSeries.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataLogSeries>, I>>(object: I): DataLogSeries {
    const message = createBaseDataLogSeries();
    message.column = object.column ?? 0;
    message.name = object.name ?? "";
    message.shortName = object.shortName ?? "";
    message.unit = object.unit ?? "";
    message.kind = object.kind ?? 0;
    message.canonicalId = object.canonicalId ?? "";
    message.min = object.min ?? 0;
    message.max = object.max ?? 0;
    message.sampleCount = object.sampleCount ?? 0;
    return message;
  },
};

function createBaseDataLog(): DataLog {
  return {
    id: undefined,
    format: 0,
    parserVersion: 0,
    source: undefined,
    start: undefined,
    utcOffsetMinutes: 0,
    durationSeconds: 0,
    sampleCount: 0,
    sampleRateHz: 0,
    series: [],
    rawFile: undefined,
    encoding: 0,
    rawSha256: "",
    rawSizeBytes: 0,
    fileName: "",
    identityMismatch: false,
    airborne: false,
    startLocationIdent: "",
    endLatitude: 0,
    endLongitude: 0,
    endLocationIdent: "",
    createdAt: undefined,
    createdBy: undefined,
  };
}

export const DataLog: MessageFns<DataLog> = {
  encode(message: DataLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      DataLogId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.format !== 0) {
      writer.uint32(16).int32(message.format);
    }
    if (message.parserVersion !== 0) {
      writer.uint32(24).int32(message.parserVersion);
    }
    if (message.source !== undefined) {
      DataLogSource.encode(message.source, writer.uint32(34).fork()).join();
    }
    if (message.start !== undefined) {
      Timestamp.encode(toTimestamp(message.start), writer.uint32(42).fork()).join();
    }
    if (message.utcOffsetMinutes !== 0) {
      writer.uint32(48).int32(message.utcOffsetMinutes);
    }
    if (message.durationSeconds !== 0) {
      writer.uint32(56).int32(message.durationSeconds);
    }
    if (message.sampleCount !== 0) {
      writer.uint32(64).int32(message.sampleCount);
    }
    if (message.sampleRateHz !== 0) {
      writer.uint32(77).float(message.sampleRateHz);
    }
    for (const v of message.series) {
      DataLogSeries.encode(v!, writer.uint32(82).fork()).join();
    }
    if (message.rawFile !== undefined) {
      Attachment.encode(message.rawFile, writer.uint32(90).fork()).join();
    }
    if (message.encoding !== 0) {
      writer.uint32(96).int32(message.encoding);
    }
    if (message.rawSha256 !== "") {
      writer.uint32(106).string(message.rawSha256);
    }
    if (message.rawSizeBytes !== 0) {
      writer.uint32(112).int64(message.rawSizeBytes);
    }
    if (message.fileName !== "") {
      writer.uint32(122).string(message.fileName);
    }
    if (message.identityMismatch !== false) {
      writer.uint32(128).bool(message.identityMismatch);
    }
    if (message.airborne !== false) {
      writer.uint32(136).bool(message.airborne);
    }
    if (message.startLocationIdent !== "") {
      writer.uint32(146).string(message.startLocationIdent);
    }
    if (message.endLatitude !== 0) {
      writer.uint32(153).double(message.endLatitude);
    }
    if (message.endLongitude !== 0) {
      writer.uint32(161).double(message.endLongitude);
    }
    if (message.endLocationIdent !== "") {
      writer.uint32(170).string(message.endLocationIdent);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(178).fork()).join();
    }
    if (message.createdBy !== undefined) {
      UserId.encode(message.createdBy, writer.uint32(186).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = DataLogId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.format = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.parserVersion = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.source = DataLogSource.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.start = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.utcOffsetMinutes = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.durationSeconds = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.sampleCount = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 77) {
            break;
          }

          message.sampleRateHz = reader.float();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.series.push(DataLogSeries.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.rawFile = Attachment.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.encoding = reader.int32() as any;
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.rawSha256 = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.rawSizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.identityMismatch = reader.bool();
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.airborne = reader.bool();
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.startLocationIdent = reader.string();
          continue;
        }
        case 19: {
          if (tag !== 153) {
            break;
          }

          message.endLatitude = reader.double();
          continue;
        }
        case 20: {
          if (tag !== 161) {
            break;
          }

          message.endLongitude = reader.double();
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.endLocationIdent = reader.string();
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.createdBy = UserId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataLog {
    return {
      id: isSet(object.id) ? DataLogId.fromJSON(object.id) : undefined,
      format: isSet(object.format) ? dataLogFormatFromJSON(object.format) : 0,
      parserVersion: isSet(object.parserVersion)
        ? globalThis.Number(object.parserVersion)
        : isSet(object.parser_version)
        ? globalThis.Number(object.parser_version)
        : 0,
      source: isSet(object.source) ? DataLogSource.fromJSON(object.source) : undefined,
      start: isSet(object.start) ? fromJsonTimestamp(object.start) : undefined,
      utcOffsetMinutes: isSet(object.utcOffsetMinutes)
        ? globalThis.Number(object.utcOffsetMinutes)
        : isSet(object.utc_offset_minutes)
        ? globalThis.Number(object.utc_offset_minutes)
        : 0,
      durationSeconds: isSet(object.durationSeconds)
        ? globalThis.Number(object.durationSeconds)
        : isSet(object.duration_seconds)
        ? globalThis.Number(object.duration_seconds)
        : 0,
      sampleCount: isSet(object.sampleCount)
        ? globalThis.Number(object.sampleCount)
        : isSet(object.sample_count)
        ? globalThis.Number(object.sample_count)
        : 0,
      sampleRateHz: isSet(object.sampleRateHz)
        ? globalThis.Number(object.sampleRateHz)
        : isSet(object.sample_rate_hz)
        ? globalThis.Number(object.sample_rate_hz)
        : 0,
      series: globalThis.Array.isArray(object?.series)
        ? object.series.map((e: any) => DataLogSeries.fromJSON(e))
        : [],
      rawFile: isSet(object.rawFile)
        ? Attachment.fromJSON(object.rawFile)
        : isSet(object.raw_file)
        ? Attachment.fromJSON(object.raw_file)
        : undefined,
      encoding: isSet(object.encoding) ? dataLogEncodingFromJSON(object.encoding) : 0,
      rawSha256: isSet(object.rawSha256)
        ? globalThis.String(object.rawSha256)
        : isSet(object.raw_sha256)
        ? globalThis.String(object.raw_sha256)
        : "",
      rawSizeBytes: isSet(object.rawSizeBytes)
        ? globalThis.Number(object.rawSizeBytes)
        : isSet(object.raw_size_bytes)
        ? globalThis.Number(object.raw_size_bytes)
        : 0,
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      identityMismatch: isSet(object.identityMismatch)
        ? globalThis.Boolean(object.identityMismatch)
        : isSet(object.identity_mismatch)
        ? globalThis.Boolean(object.identity_mismatch)
        : false,
      airborne: isSet(object.airborne) ? globalThis.Boolean(object.airborne) : false,
      startLocationIdent: isSet(object.startLocationIdent)
        ? globalThis.String(object.startLocationIdent)
        : isSet(object.start_location_ident)
        ? globalThis.String(object.start_location_ident)
        : "",
      endLatitude: isSet(object.endLatitude)
        ? globalThis.Number(object.endLatitude)
        : isSet(object.end_latitude)
        ? globalThis.Number(object.end_latitude)
        : 0,
      endLongitude: isSet(object.endLongitude)
        ? globalThis.Number(object.endLongitude)
        : isSet(object.end_longitude)
        ? globalThis.Number(object.end_longitude)
        : 0,
      endLocationIdent: isSet(object.endLocationIdent)
        ? globalThis.String(object.endLocationIdent)
        : isSet(object.end_location_ident)
        ? globalThis.String(object.end_location_ident)
        : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      createdBy: isSet(object.createdBy)
        ? UserId.fromJSON(object.createdBy)
        : isSet(object.created_by)
        ? UserId.fromJSON(object.created_by)
        : undefined,
    };
  },

  toJSON(message: DataLog): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = DataLogId.toJSON(message.id);
    }
    if (message.format !== 0) {
      obj.format = dataLogFormatToJSON(message.format);
    }
    if (message.parserVersion !== 0) {
      obj.parserVersion = Math.round(message.parserVersion);
    }
    if (message.source !== undefined) {
      obj.source = DataLogSource.toJSON(message.source);
    }
    if (message.start !== undefined) {
      obj.start = message.start.toISOString();
    }
    if (message.utcOffsetMinutes !== 0) {
      obj.utcOffsetMinutes = Math.round(message.utcOffsetMinutes);
    }
    if (message.durationSeconds !== 0) {
      obj.durationSeconds = Math.round(message.durationSeconds);
    }
    if (message.sampleCount !== 0) {
      obj.sampleCount = Math.round(message.sampleCount);
    }
    if (message.sampleRateHz !== 0) {
      obj.sampleRateHz = message.sampleRateHz;
    }
    if (message.series?.length) {
      obj.series = message.series.map((e) => DataLogSeries.toJSON(e));
    }
    if (message.rawFile !== undefined) {
      obj.rawFile = Attachment.toJSON(message.rawFile);
    }
    if (message.encoding !== 0) {
      obj.encoding = dataLogEncodingToJSON(message.encoding);
    }
    if (message.rawSha256 !== "") {
      obj.rawSha256 = message.rawSha256;
    }
    if (message.rawSizeBytes !== 0) {
      obj.rawSizeBytes = Math.round(message.rawSizeBytes);
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.identityMismatch !== false) {
      obj.identityMismatch = message.identityMismatch;
    }
    if (message.airborne !== false) {
      obj.airborne = message.airborne;
    }
    if (message.startLocationIdent !== "") {
      obj.startLocationIdent = message.startLocationIdent;
    }
    if (message.endLatitude !== 0) {
      obj.endLatitude = message.endLatitude;
    }
    if (message.endLongitude !== 0) {
      obj.endLongitude = message.endLongitude;
    }
    if (message.endLocationIdent !== "") {
      obj.endLocationIdent = message.endLocationIdent;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.createdBy !== undefined) {
      obj.createdBy = UserId.toJSON(message.createdBy);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataLog>, I>>(base?: I): DataLog {
    return DataLog.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataLog>, I>>(object: I): DataLog {
    const message = createBaseDataLog();
    message.id = (object.id !== undefined && object.id !== null) ? DataLogId.fromPartial(object.id) : undefined;
    message.format = object.format ?? 0;
    message.parserVersion = object.parserVersion ?? 0;
    message.source = (object.source !== undefined && object.source !== null)
      ? DataLogSource.fromPartial(object.source)
      : undefined;
    message.start = object.start ?? undefined;
    message.utcOffsetMinutes = object.utcOffsetMinutes ?? 0;
    message.durationSeconds = object.durationSeconds ?? 0;
    message.sampleCount = object.sampleCount ?? 0;
    message.sampleRateHz = object.sampleRateHz ?? 0;
    message.series = object.series?.map((e) => DataLogSeries.fromPartial(e)) || [];
    message.rawFile = (object.rawFile !== undefined && object.rawFile !== null)
      ? Attachment.fromPartial(object.rawFile)
      : undefined;
    message.encoding = object.encoding ?? 0;
    message.rawSha256 = object.rawSha256 ?? "";
    message.rawSizeBytes = object.rawSizeBytes ?? 0;
    message.fileName = object.fileName ?? "";
    message.identityMismatch = object.identityMismatch ?? false;
    message.airborne = object.airborne ?? false;
    message.startLocationIdent = object.startLocationIdent ?? "";
    message.endLatitude = object.endLatitude ?? 0;
    message.endLongitude = object.endLongitude ?? 0;
    message.endLocationIdent = object.endLocationIdent ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.createdBy = (object.createdBy !== undefined && object.createdBy !== null)
      ? UserId.fromPartial(object.createdBy)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
