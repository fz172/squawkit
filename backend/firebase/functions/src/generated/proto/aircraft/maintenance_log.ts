// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: aircraft/maintenance_log.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { Attachment } from "./attachment";
import { ComponentType, componentTypeFromJSON, componentTypeToJSON } from "./component_type";
import { Technician } from "./technician";

export const protobufPackage = "";

export interface MaintenanceLog {
  id: string;
  timestamp: Date | undefined;
  technicianId: string;
  workDescription: string;
  componentType: ComponentType;
  /** References Engine/Prop/Blade Serial */
  componentSerial: string;
  engineHour: number;
  /**
   * Field 9 repurposed from repeated string attachment_urls.
   * Safe because no data was ever written to field 9 before this change.
   */
  attachments: Attachment[];
  /** Separate time tracking for each component category */
  airframeTime: number;
  propTime: number;
  /** Inspection cards associated with this log entry */
  inspectionIds: string[];
  /** Technician who performed the work */
  technician:
    | Technician
    | undefined;
  /** Squawks addressed by this log entry */
  squawkIds: string[];
}

function createBaseMaintenanceLog(): MaintenanceLog {
  return {
    id: "",
    timestamp: undefined,
    technicianId: "",
    workDescription: "",
    componentType: 0,
    componentSerial: "",
    engineHour: 0,
    attachments: [],
    airframeTime: 0,
    propTime: 0,
    inspectionIds: [],
    technician: undefined,
    squawkIds: [],
  };
}

export const MaintenanceLog: MessageFns<MaintenanceLog> = {
  encode(message: MaintenanceLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(18).fork()).join();
    }
    if (message.technicianId !== "") {
      writer.uint32(26).string(message.technicianId);
    }
    if (message.workDescription !== "") {
      writer.uint32(34).string(message.workDescription);
    }
    if (message.componentType !== 0) {
      writer.uint32(40).int32(message.componentType);
    }
    if (message.componentSerial !== "") {
      writer.uint32(50).string(message.componentSerial);
    }
    if (message.engineHour !== 0) {
      writer.uint32(65).double(message.engineHour);
    }
    for (const v of message.attachments) {
      Attachment.encode(v!, writer.uint32(74).fork()).join();
    }
    if (message.airframeTime !== 0) {
      writer.uint32(81).double(message.airframeTime);
    }
    if (message.propTime !== 0) {
      writer.uint32(89).double(message.propTime);
    }
    for (const v of message.inspectionIds) {
      writer.uint32(98).string(v!);
    }
    if (message.technician !== undefined) {
      Technician.encode(message.technician, writer.uint32(106).fork()).join();
    }
    for (const v of message.squawkIds) {
      writer.uint32(114).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MaintenanceLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMaintenanceLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.technicianId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.workDescription = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.componentType = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.componentSerial = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 65) {
            break;
          }

          message.engineHour = reader.double();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.attachments.push(Attachment.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 81) {
            break;
          }

          message.airframeTime = reader.double();
          continue;
        }
        case 11: {
          if (tag !== 89) {
            break;
          }

          message.propTime = reader.double();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.inspectionIds.push(reader.string());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.technician = Technician.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.squawkIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MaintenanceLog {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      technicianId: isSet(object.technicianId)
        ? globalThis.String(object.technicianId)
        : isSet(object.technician_id)
        ? globalThis.String(object.technician_id)
        : "",
      workDescription: isSet(object.workDescription)
        ? globalThis.String(object.workDescription)
        : isSet(object.work_description)
        ? globalThis.String(object.work_description)
        : "",
      componentType: isSet(object.componentType)
        ? componentTypeFromJSON(object.componentType)
        : isSet(object.component_type)
        ? componentTypeFromJSON(object.component_type)
        : 0,
      componentSerial: isSet(object.componentSerial)
        ? globalThis.String(object.componentSerial)
        : isSet(object.component_serial)
        ? globalThis.String(object.component_serial)
        : "",
      engineHour: isSet(object.engineHour)
        ? globalThis.Number(object.engineHour)
        : isSet(object.engine_hour)
        ? globalThis.Number(object.engine_hour)
        : 0,
      attachments: globalThis.Array.isArray(object?.attachments)
        ? object.attachments.map((e: any) => Attachment.fromJSON(e))
        : [],
      airframeTime: isSet(object.airframeTime)
        ? globalThis.Number(object.airframeTime)
        : isSet(object.airframe_time)
        ? globalThis.Number(object.airframe_time)
        : 0,
      propTime: isSet(object.propTime)
        ? globalThis.Number(object.propTime)
        : isSet(object.prop_time)
        ? globalThis.Number(object.prop_time)
        : 0,
      inspectionIds: globalThis.Array.isArray(object?.inspectionIds)
        ? object.inspectionIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.inspection_ids)
        ? object.inspection_ids.map((e: any) => globalThis.String(e))
        : [],
      technician: isSet(object.technician) ? Technician.fromJSON(object.technician) : undefined,
      squawkIds: globalThis.Array.isArray(object?.squawkIds)
        ? object.squawkIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.squawk_ids)
        ? object.squawk_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: MaintenanceLog): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.technicianId !== "") {
      obj.technicianId = message.technicianId;
    }
    if (message.workDescription !== "") {
      obj.workDescription = message.workDescription;
    }
    if (message.componentType !== 0) {
      obj.componentType = componentTypeToJSON(message.componentType);
    }
    if (message.componentSerial !== "") {
      obj.componentSerial = message.componentSerial;
    }
    if (message.engineHour !== 0) {
      obj.engineHour = message.engineHour;
    }
    if (message.attachments?.length) {
      obj.attachments = message.attachments.map((e) => Attachment.toJSON(e));
    }
    if (message.airframeTime !== 0) {
      obj.airframeTime = message.airframeTime;
    }
    if (message.propTime !== 0) {
      obj.propTime = message.propTime;
    }
    if (message.inspectionIds?.length) {
      obj.inspectionIds = message.inspectionIds;
    }
    if (message.technician !== undefined) {
      obj.technician = Technician.toJSON(message.technician);
    }
    if (message.squawkIds?.length) {
      obj.squawkIds = message.squawkIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MaintenanceLog>, I>>(base?: I): MaintenanceLog {
    return MaintenanceLog.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MaintenanceLog>, I>>(object: I): MaintenanceLog {
    const message = createBaseMaintenanceLog();
    message.id = object.id ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.technicianId = object.technicianId ?? "";
    message.workDescription = object.workDescription ?? "";
    message.componentType = object.componentType ?? 0;
    message.componentSerial = object.componentSerial ?? "";
    message.engineHour = object.engineHour ?? 0;
    message.attachments = object.attachments?.map((e) => Attachment.fromPartial(e)) || [];
    message.airframeTime = object.airframeTime ?? 0;
    message.propTime = object.propTime ?? 0;
    message.inspectionIds = object.inspectionIds?.map((e) => e) || [];
    message.technician = (object.technician !== undefined && object.technician !== null)
      ? Technician.fromPartial(object.technician)
      : undefined;
    message.squawkIds = object.squawkIds?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
