// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: aircraft/squawk.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { Attachment } from "./attachment";
import { ComponentType, componentTypeFromJSON, componentTypeToJSON } from "./component_type";

export const protobufPackage = "";

export enum SquawkPriority {
  SQUAWK_PRIORITY_UNKNOWN = 0,
  SQUAWK_PRIORITY_LOW = 1,
  SQUAWK_PRIORITY_MEDIUM = 2,
  SQUAWK_PRIORITY_HIGH = 3,
  SQUAWK_PRIORITY_AOG = 4,
  UNRECOGNIZED = -1,
}

export function squawkPriorityFromJSON(object: any): SquawkPriority {
  switch (object) {
    case 0:
    case "SQUAWK_PRIORITY_UNKNOWN":
      return SquawkPriority.SQUAWK_PRIORITY_UNKNOWN;
    case 1:
    case "SQUAWK_PRIORITY_LOW":
      return SquawkPriority.SQUAWK_PRIORITY_LOW;
    case 2:
    case "SQUAWK_PRIORITY_MEDIUM":
      return SquawkPriority.SQUAWK_PRIORITY_MEDIUM;
    case 3:
    case "SQUAWK_PRIORITY_HIGH":
      return SquawkPriority.SQUAWK_PRIORITY_HIGH;
    case 4:
    case "SQUAWK_PRIORITY_AOG":
      return SquawkPriority.SQUAWK_PRIORITY_AOG;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SquawkPriority.UNRECOGNIZED;
  }
}

export function squawkPriorityToJSON(object: SquawkPriority): string {
  switch (object) {
    case SquawkPriority.SQUAWK_PRIORITY_UNKNOWN:
      return "SQUAWK_PRIORITY_UNKNOWN";
    case SquawkPriority.SQUAWK_PRIORITY_LOW:
      return "SQUAWK_PRIORITY_LOW";
    case SquawkPriority.SQUAWK_PRIORITY_MEDIUM:
      return "SQUAWK_PRIORITY_MEDIUM";
    case SquawkPriority.SQUAWK_PRIORITY_HIGH:
      return "SQUAWK_PRIORITY_HIGH";
    case SquawkPriority.SQUAWK_PRIORITY_AOG:
      return "SQUAWK_PRIORITY_AOG";
    case SquawkPriority.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum SquawkDismissReason {
  SQUAWK_DISMISS_REASON_UNKNOWN = 0,
  SQUAWK_DISMISS_REASON_OBSOLETE = 1,
  SQUAWK_DISMISS_REASON_NOT_REPRODUCIBLE = 2,
  SQUAWK_DISMISS_REASON_DUPLICATE = 3,
  SQUAWK_DISMISS_REASON_INTENDED_BEHAVIOR = 4,
  UNRECOGNIZED = -1,
}

export function squawkDismissReasonFromJSON(object: any): SquawkDismissReason {
  switch (object) {
    case 0:
    case "SQUAWK_DISMISS_REASON_UNKNOWN":
      return SquawkDismissReason.SQUAWK_DISMISS_REASON_UNKNOWN;
    case 1:
    case "SQUAWK_DISMISS_REASON_OBSOLETE":
      return SquawkDismissReason.SQUAWK_DISMISS_REASON_OBSOLETE;
    case 2:
    case "SQUAWK_DISMISS_REASON_NOT_REPRODUCIBLE":
      return SquawkDismissReason.SQUAWK_DISMISS_REASON_NOT_REPRODUCIBLE;
    case 3:
    case "SQUAWK_DISMISS_REASON_DUPLICATE":
      return SquawkDismissReason.SQUAWK_DISMISS_REASON_DUPLICATE;
    case 4:
    case "SQUAWK_DISMISS_REASON_INTENDED_BEHAVIOR":
      return SquawkDismissReason.SQUAWK_DISMISS_REASON_INTENDED_BEHAVIOR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SquawkDismissReason.UNRECOGNIZED;
  }
}

export function squawkDismissReasonToJSON(object: SquawkDismissReason): string {
  switch (object) {
    case SquawkDismissReason.SQUAWK_DISMISS_REASON_UNKNOWN:
      return "SQUAWK_DISMISS_REASON_UNKNOWN";
    case SquawkDismissReason.SQUAWK_DISMISS_REASON_OBSOLETE:
      return "SQUAWK_DISMISS_REASON_OBSOLETE";
    case SquawkDismissReason.SQUAWK_DISMISS_REASON_NOT_REPRODUCIBLE:
      return "SQUAWK_DISMISS_REASON_NOT_REPRODUCIBLE";
    case SquawkDismissReason.SQUAWK_DISMISS_REASON_DUPLICATE:
      return "SQUAWK_DISMISS_REASON_DUPLICATE";
    case SquawkDismissReason.SQUAWK_DISMISS_REASON_INTENDED_BEHAVIOR:
      return "SQUAWK_DISMISS_REASON_INTENDED_BEHAVIOR";
    case SquawkDismissReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Squawk {
  id: string;
  title: string;
  description: string;
  priority: SquawkPriority;
  createdAt: Date | undefined;
  componentType: ComponentType;
  componentSerial: string;
  attachments: Attachment[];
  /** Empty = Open; populated = Addressed. */
  addressedByLogId: string;
  /** Non-zero = Dismissed without corrective action. */
  dismissReason: SquawkDismissReason;
  dismissedAt: Date | undefined;
}

function createBaseSquawk(): Squawk {
  return {
    id: "",
    title: "",
    description: "",
    priority: 0,
    createdAt: undefined,
    componentType: 0,
    componentSerial: "",
    attachments: [],
    addressedByLogId: "",
    dismissReason: 0,
    dismissedAt: undefined,
  };
}

export const Squawk: MessageFns<Squawk> = {
  encode(message: Squawk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.priority !== 0) {
      writer.uint32(32).int32(message.priority);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(42).fork()).join();
    }
    if (message.componentType !== 0) {
      writer.uint32(48).int32(message.componentType);
    }
    if (message.componentSerial !== "") {
      writer.uint32(58).string(message.componentSerial);
    }
    for (const v of message.attachments) {
      Attachment.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.addressedByLogId !== "") {
      writer.uint32(74).string(message.addressedByLogId);
    }
    if (message.dismissReason !== 0) {
      writer.uint32(80).int32(message.dismissReason);
    }
    if (message.dismissedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.dismissedAt), writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Squawk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSquawk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.priority = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.componentType = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.componentSerial = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.attachments.push(Attachment.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.addressedByLogId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.dismissReason = reader.int32() as any;
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.dismissedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Squawk {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      priority: isSet(object.priority) ? squawkPriorityFromJSON(object.priority) : 0,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      componentType: isSet(object.componentType)
        ? componentTypeFromJSON(object.componentType)
        : isSet(object.component_type)
        ? componentTypeFromJSON(object.component_type)
        : 0,
      componentSerial: isSet(object.componentSerial)
        ? globalThis.String(object.componentSerial)
        : isSet(object.component_serial)
        ? globalThis.String(object.component_serial)
        : "",
      attachments: globalThis.Array.isArray(object?.attachments)
        ? object.attachments.map((e: any) => Attachment.fromJSON(e))
        : [],
      addressedByLogId: isSet(object.addressedByLogId)
        ? globalThis.String(object.addressedByLogId)
        : isSet(object.addressed_by_log_id)
        ? globalThis.String(object.addressed_by_log_id)
        : "",
      dismissReason: isSet(object.dismissReason)
        ? squawkDismissReasonFromJSON(object.dismissReason)
        : isSet(object.dismiss_reason)
        ? squawkDismissReasonFromJSON(object.dismiss_reason)
        : 0,
      dismissedAt: isSet(object.dismissedAt)
        ? fromJsonTimestamp(object.dismissedAt)
        : isSet(object.dismissed_at)
        ? fromJsonTimestamp(object.dismissed_at)
        : undefined,
    };
  },

  toJSON(message: Squawk): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.priority !== 0) {
      obj.priority = squawkPriorityToJSON(message.priority);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.componentType !== 0) {
      obj.componentType = componentTypeToJSON(message.componentType);
    }
    if (message.componentSerial !== "") {
      obj.componentSerial = message.componentSerial;
    }
    if (message.attachments?.length) {
      obj.attachments = message.attachments.map((e) => Attachment.toJSON(e));
    }
    if (message.addressedByLogId !== "") {
      obj.addressedByLogId = message.addressedByLogId;
    }
    if (message.dismissReason !== 0) {
      obj.dismissReason = squawkDismissReasonToJSON(message.dismissReason);
    }
    if (message.dismissedAt !== undefined) {
      obj.dismissedAt = message.dismissedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Squawk>, I>>(base?: I): Squawk {
    return Squawk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Squawk>, I>>(object: I): Squawk {
    const message = createBaseSquawk();
    message.id = object.id ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.priority = object.priority ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.componentType = object.componentType ?? 0;
    message.componentSerial = object.componentSerial ?? "";
    message.attachments = object.attachments?.map((e) => Attachment.fromPartial(e)) || [];
    message.addressedByLogId = object.addressedByLogId ?? "";
    message.dismissReason = object.dismissReason ?? 0;
    message.dismissedAt = object.dismissedAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
