// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: aircraft/maintenance_task.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";
import { Attachment } from "./attachment";
import { ComponentType, componentTypeFromJSON, componentTypeToJSON } from "./component_type";

export const protobufPackage = "";

export enum ComplianceType {
  COMPLIANCE_TYPE_ROUTINE_INSPECTION = 0,
  COMPLIANCE_TYPE_SERVICE_BULLETIN = 1,
  COMPLIANCE_TYPE_AIRWORTHINESS_DIRECTIVE = 2,
  UNRECOGNIZED = -1,
}

export function complianceTypeFromJSON(object: any): ComplianceType {
  switch (object) {
    case 0:
    case "COMPLIANCE_TYPE_ROUTINE_INSPECTION":
      return ComplianceType.COMPLIANCE_TYPE_ROUTINE_INSPECTION;
    case 1:
    case "COMPLIANCE_TYPE_SERVICE_BULLETIN":
      return ComplianceType.COMPLIANCE_TYPE_SERVICE_BULLETIN;
    case 2:
    case "COMPLIANCE_TYPE_AIRWORTHINESS_DIRECTIVE":
      return ComplianceType.COMPLIANCE_TYPE_AIRWORTHINESS_DIRECTIVE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ComplianceType.UNRECOGNIZED;
  }
}

export function complianceTypeToJSON(object: ComplianceType): string {
  switch (object) {
    case ComplianceType.COMPLIANCE_TYPE_ROUTINE_INSPECTION:
      return "COMPLIANCE_TYPE_ROUTINE_INSPECTION";
    case ComplianceType.COMPLIANCE_TYPE_SERVICE_BULLETIN:
      return "COMPLIANCE_TYPE_SERVICE_BULLETIN";
    case ComplianceType.COMPLIANCE_TYPE_AIRWORTHINESS_DIRECTIVE:
      return "COMPLIANCE_TYPE_AIRWORTHINESS_DIRECTIVE";
    case ComplianceType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * A time-interval-based rule (e.g. every 12 months).
 * Exactly one of interval_days / interval_months / interval_years is set.
 * Older payloads only set interval_months — readers that see all three zero
 * should treat months as the unit.
 */
export interface TimeRule {
  intervalMonths: number;
  creationDate: Date | undefined;
  intervalDays: number;
  intervalYears: number;
}

/** A engine-hour-based rule (e.g. every 100 hours) */
export interface EngineHourRule {
  intervalHours: number;
}

/** Inspection is performed on condition (no fixed interval) */
export interface OnConditionRule {
  description: string;
}

export interface LinkedRule {
  /** e.g., ID of the "Annual" card */
  parentInspectionId: string;
}

export interface ImmediateRule {
}

/** A single rule governing when an inspection is due */
export interface InspectionRule {
  timeRule?: TimeRule | undefined;
  engineHourRule?: EngineHourRule | undefined;
  onConditionRule?: OnConditionRule | undefined;
  linkedRule?: LinkedRule | undefined;
  immediateRule?: ImmediateRule | undefined;
}

/** Status to track when an inspection was forcefully marked as complied with */
export interface ForceCompliedStatus {
  compliedDate: Date | undefined;
  compliedEngineHours: number;
}

/** A configured maintenance task card for an aircraft */
export interface MaintenanceTask {
  id: string;
  title: string;
  component: ComponentType;
  rules: InspectionRule[];
  /** Optional force overrides — when set, skip computed due calculation */
  forceDueDate: Date | undefined;
  forceDueEngineHour: number;
  /** Optional free-text notes or comments for this maintenance task */
  notes: string;
  /** v2 Compliance Tracking */
  type: ComplianceType;
  /** e.g., "SB-2024-01" */
  referenceNumber: string;
  complianceAuthority: string;
  complianceDetails: string;
  /** If true, moves to history after first log */
  isOneTime: boolean;
  attachments: Attachment[];
  forceCompliedStatus: ForceCompliedStatus | undefined;
}

function createBaseTimeRule(): TimeRule {
  return { intervalMonths: 0, creationDate: undefined, intervalDays: 0, intervalYears: 0 };
}

export const TimeRule: MessageFns<TimeRule> = {
  encode(message: TimeRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.intervalMonths !== 0) {
      writer.uint32(8).int32(message.intervalMonths);
    }
    if (message.creationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.creationDate), writer.uint32(18).fork()).join();
    }
    if (message.intervalDays !== 0) {
      writer.uint32(24).int32(message.intervalDays);
    }
    if (message.intervalYears !== 0) {
      writer.uint32(32).int32(message.intervalYears);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TimeRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.intervalMonths = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.creationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.intervalDays = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.intervalYears = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeRule {
    return {
      intervalMonths: isSet(object.intervalMonths)
        ? globalThis.Number(object.intervalMonths)
        : isSet(object.interval_months)
        ? globalThis.Number(object.interval_months)
        : 0,
      creationDate: isSet(object.creationDate)
        ? fromJsonTimestamp(object.creationDate)
        : isSet(object.creation_date)
        ? fromJsonTimestamp(object.creation_date)
        : undefined,
      intervalDays: isSet(object.intervalDays)
        ? globalThis.Number(object.intervalDays)
        : isSet(object.interval_days)
        ? globalThis.Number(object.interval_days)
        : 0,
      intervalYears: isSet(object.intervalYears)
        ? globalThis.Number(object.intervalYears)
        : isSet(object.interval_years)
        ? globalThis.Number(object.interval_years)
        : 0,
    };
  },

  toJSON(message: TimeRule): unknown {
    const obj: any = {};
    if (message.intervalMonths !== 0) {
      obj.intervalMonths = Math.round(message.intervalMonths);
    }
    if (message.creationDate !== undefined) {
      obj.creationDate = message.creationDate.toISOString();
    }
    if (message.intervalDays !== 0) {
      obj.intervalDays = Math.round(message.intervalDays);
    }
    if (message.intervalYears !== 0) {
      obj.intervalYears = Math.round(message.intervalYears);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimeRule>, I>>(base?: I): TimeRule {
    return TimeRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimeRule>, I>>(object: I): TimeRule {
    const message = createBaseTimeRule();
    message.intervalMonths = object.intervalMonths ?? 0;
    message.creationDate = object.creationDate ?? undefined;
    message.intervalDays = object.intervalDays ?? 0;
    message.intervalYears = object.intervalYears ?? 0;
    return message;
  },
};

function createBaseEngineHourRule(): EngineHourRule {
  return { intervalHours: 0 };
}

export const EngineHourRule: MessageFns<EngineHourRule> = {
  encode(message: EngineHourRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.intervalHours !== 0) {
      writer.uint32(13).float(message.intervalHours);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EngineHourRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEngineHourRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.intervalHours = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EngineHourRule {
    return {
      intervalHours: isSet(object.intervalHours)
        ? globalThis.Number(object.intervalHours)
        : isSet(object.interval_hours)
        ? globalThis.Number(object.interval_hours)
        : 0,
    };
  },

  toJSON(message: EngineHourRule): unknown {
    const obj: any = {};
    if (message.intervalHours !== 0) {
      obj.intervalHours = message.intervalHours;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EngineHourRule>, I>>(base?: I): EngineHourRule {
    return EngineHourRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EngineHourRule>, I>>(object: I): EngineHourRule {
    const message = createBaseEngineHourRule();
    message.intervalHours = object.intervalHours ?? 0;
    return message;
  },
};

function createBaseOnConditionRule(): OnConditionRule {
  return { description: "" };
}

export const OnConditionRule: MessageFns<OnConditionRule> = {
  encode(message: OnConditionRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.description !== "") {
      writer.uint32(10).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OnConditionRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOnConditionRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OnConditionRule {
    return { description: isSet(object.description) ? globalThis.String(object.description) : "" };
  },

  toJSON(message: OnConditionRule): unknown {
    const obj: any = {};
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<OnConditionRule>, I>>(base?: I): OnConditionRule {
    return OnConditionRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OnConditionRule>, I>>(object: I): OnConditionRule {
    const message = createBaseOnConditionRule();
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseLinkedRule(): LinkedRule {
  return { parentInspectionId: "" };
}

export const LinkedRule: MessageFns<LinkedRule> = {
  encode(message: LinkedRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.parentInspectionId !== "") {
      writer.uint32(10).string(message.parentInspectionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LinkedRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLinkedRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.parentInspectionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LinkedRule {
    return {
      parentInspectionId: isSet(object.parentInspectionId)
        ? globalThis.String(object.parentInspectionId)
        : isSet(object.parent_inspection_id)
        ? globalThis.String(object.parent_inspection_id)
        : "",
    };
  },

  toJSON(message: LinkedRule): unknown {
    const obj: any = {};
    if (message.parentInspectionId !== "") {
      obj.parentInspectionId = message.parentInspectionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LinkedRule>, I>>(base?: I): LinkedRule {
    return LinkedRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LinkedRule>, I>>(object: I): LinkedRule {
    const message = createBaseLinkedRule();
    message.parentInspectionId = object.parentInspectionId ?? "";
    return message;
  },
};

function createBaseImmediateRule(): ImmediateRule {
  return {};
}

export const ImmediateRule: MessageFns<ImmediateRule> = {
  encode(_: ImmediateRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImmediateRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImmediateRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ImmediateRule {
    return {};
  },

  toJSON(_: ImmediateRule): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ImmediateRule>, I>>(base?: I): ImmediateRule {
    return ImmediateRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImmediateRule>, I>>(_: I): ImmediateRule {
    const message = createBaseImmediateRule();
    return message;
  },
};

function createBaseInspectionRule(): InspectionRule {
  return {
    timeRule: undefined,
    engineHourRule: undefined,
    onConditionRule: undefined,
    linkedRule: undefined,
    immediateRule: undefined,
  };
}

export const InspectionRule: MessageFns<InspectionRule> = {
  encode(message: InspectionRule, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timeRule !== undefined) {
      TimeRule.encode(message.timeRule, writer.uint32(10).fork()).join();
    }
    if (message.engineHourRule !== undefined) {
      EngineHourRule.encode(message.engineHourRule, writer.uint32(18).fork()).join();
    }
    if (message.onConditionRule !== undefined) {
      OnConditionRule.encode(message.onConditionRule, writer.uint32(26).fork()).join();
    }
    if (message.linkedRule !== undefined) {
      LinkedRule.encode(message.linkedRule, writer.uint32(34).fork()).join();
    }
    if (message.immediateRule !== undefined) {
      ImmediateRule.encode(message.immediateRule, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InspectionRule {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInspectionRule();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timeRule = TimeRule.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.engineHourRule = EngineHourRule.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.onConditionRule = OnConditionRule.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.linkedRule = LinkedRule.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.immediateRule = ImmediateRule.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InspectionRule {
    return {
      timeRule: isSet(object.timeRule)
        ? TimeRule.fromJSON(object.timeRule)
        : isSet(object.time_rule)
        ? TimeRule.fromJSON(object.time_rule)
        : undefined,
      engineHourRule: isSet(object.engineHourRule)
        ? EngineHourRule.fromJSON(object.engineHourRule)
        : isSet(object.engine_hour_rule)
        ? EngineHourRule.fromJSON(object.engine_hour_rule)
        : undefined,
      onConditionRule: isSet(object.onConditionRule)
        ? OnConditionRule.fromJSON(object.onConditionRule)
        : isSet(object.on_condition_rule)
        ? OnConditionRule.fromJSON(object.on_condition_rule)
        : undefined,
      linkedRule: isSet(object.linkedRule)
        ? LinkedRule.fromJSON(object.linkedRule)
        : isSet(object.linked_rule)
        ? LinkedRule.fromJSON(object.linked_rule)
        : undefined,
      immediateRule: isSet(object.immediateRule)
        ? ImmediateRule.fromJSON(object.immediateRule)
        : isSet(object.immediate_rule)
        ? ImmediateRule.fromJSON(object.immediate_rule)
        : undefined,
    };
  },

  toJSON(message: InspectionRule): unknown {
    const obj: any = {};
    if (message.timeRule !== undefined) {
      obj.timeRule = TimeRule.toJSON(message.timeRule);
    }
    if (message.engineHourRule !== undefined) {
      obj.engineHourRule = EngineHourRule.toJSON(message.engineHourRule);
    }
    if (message.onConditionRule !== undefined) {
      obj.onConditionRule = OnConditionRule.toJSON(message.onConditionRule);
    }
    if (message.linkedRule !== undefined) {
      obj.linkedRule = LinkedRule.toJSON(message.linkedRule);
    }
    if (message.immediateRule !== undefined) {
      obj.immediateRule = ImmediateRule.toJSON(message.immediateRule);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InspectionRule>, I>>(base?: I): InspectionRule {
    return InspectionRule.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InspectionRule>, I>>(object: I): InspectionRule {
    const message = createBaseInspectionRule();
    message.timeRule = (object.timeRule !== undefined && object.timeRule !== null)
      ? TimeRule.fromPartial(object.timeRule)
      : undefined;
    message.engineHourRule = (object.engineHourRule !== undefined && object.engineHourRule !== null)
      ? EngineHourRule.fromPartial(object.engineHourRule)
      : undefined;
    message.onConditionRule = (object.onConditionRule !== undefined && object.onConditionRule !== null)
      ? OnConditionRule.fromPartial(object.onConditionRule)
      : undefined;
    message.linkedRule = (object.linkedRule !== undefined && object.linkedRule !== null)
      ? LinkedRule.fromPartial(object.linkedRule)
      : undefined;
    message.immediateRule = (object.immediateRule !== undefined && object.immediateRule !== null)
      ? ImmediateRule.fromPartial(object.immediateRule)
      : undefined;
    return message;
  },
};

function createBaseForceCompliedStatus(): ForceCompliedStatus {
  return { compliedDate: undefined, compliedEngineHours: 0 };
}

export const ForceCompliedStatus: MessageFns<ForceCompliedStatus> = {
  encode(message: ForceCompliedStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.compliedDate !== undefined) {
      Timestamp.encode(toTimestamp(message.compliedDate), writer.uint32(10).fork()).join();
    }
    if (message.compliedEngineHours !== 0) {
      writer.uint32(21).float(message.compliedEngineHours);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ForceCompliedStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseForceCompliedStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.compliedDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.compliedEngineHours = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ForceCompliedStatus {
    return {
      compliedDate: isSet(object.compliedDate)
        ? fromJsonTimestamp(object.compliedDate)
        : isSet(object.complied_date)
        ? fromJsonTimestamp(object.complied_date)
        : undefined,
      compliedEngineHours: isSet(object.compliedEngineHours)
        ? globalThis.Number(object.compliedEngineHours)
        : isSet(object.complied_engine_hours)
        ? globalThis.Number(object.complied_engine_hours)
        : 0,
    };
  },

  toJSON(message: ForceCompliedStatus): unknown {
    const obj: any = {};
    if (message.compliedDate !== undefined) {
      obj.compliedDate = message.compliedDate.toISOString();
    }
    if (message.compliedEngineHours !== 0) {
      obj.compliedEngineHours = message.compliedEngineHours;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ForceCompliedStatus>, I>>(base?: I): ForceCompliedStatus {
    return ForceCompliedStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ForceCompliedStatus>, I>>(object: I): ForceCompliedStatus {
    const message = createBaseForceCompliedStatus();
    message.compliedDate = object.compliedDate ?? undefined;
    message.compliedEngineHours = object.compliedEngineHours ?? 0;
    return message;
  },
};

function createBaseMaintenanceTask(): MaintenanceTask {
  return {
    id: "",
    title: "",
    component: 0,
    rules: [],
    forceDueDate: undefined,
    forceDueEngineHour: 0,
    notes: "",
    type: 0,
    referenceNumber: "",
    complianceAuthority: "",
    complianceDetails: "",
    isOneTime: false,
    attachments: [],
    forceCompliedStatus: undefined,
  };
}

export const MaintenanceTask: MessageFns<MaintenanceTask> = {
  encode(message: MaintenanceTask, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.component !== 0) {
      writer.uint32(24).int32(message.component);
    }
    for (const v of message.rules) {
      InspectionRule.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.forceDueDate !== undefined) {
      Timestamp.encode(toTimestamp(message.forceDueDate), writer.uint32(42).fork()).join();
    }
    if (message.forceDueEngineHour !== 0) {
      writer.uint32(53).float(message.forceDueEngineHour);
    }
    if (message.notes !== "") {
      writer.uint32(58).string(message.notes);
    }
    if (message.type !== 0) {
      writer.uint32(64).int32(message.type);
    }
    if (message.referenceNumber !== "") {
      writer.uint32(74).string(message.referenceNumber);
    }
    if (message.complianceAuthority !== "") {
      writer.uint32(82).string(message.complianceAuthority);
    }
    if (message.complianceDetails !== "") {
      writer.uint32(90).string(message.complianceDetails);
    }
    if (message.isOneTime !== false) {
      writer.uint32(96).bool(message.isOneTime);
    }
    for (const v of message.attachments) {
      Attachment.encode(v!, writer.uint32(106).fork()).join();
    }
    if (message.forceCompliedStatus !== undefined) {
      ForceCompliedStatus.encode(message.forceCompliedStatus, writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MaintenanceTask {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMaintenanceTask();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.component = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rules.push(InspectionRule.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.forceDueDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.forceDueEngineHour = reader.float();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.notes = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.referenceNumber = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.complianceAuthority = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.complianceDetails = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.isOneTime = reader.bool();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.attachments.push(Attachment.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.forceCompliedStatus = ForceCompliedStatus.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MaintenanceTask {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      component: isSet(object.component) ? componentTypeFromJSON(object.component) : 0,
      rules: globalThis.Array.isArray(object?.rules) ? object.rules.map((e: any) => InspectionRule.fromJSON(e)) : [],
      forceDueDate: isSet(object.forceDueDate)
        ? fromJsonTimestamp(object.forceDueDate)
        : isSet(object.force_due_date)
        ? fromJsonTimestamp(object.force_due_date)
        : undefined,
      forceDueEngineHour: isSet(object.forceDueEngineHour)
        ? globalThis.Number(object.forceDueEngineHour)
        : isSet(object.force_due_engine_hour)
        ? globalThis.Number(object.force_due_engine_hour)
        : 0,
      notes: isSet(object.notes) ? globalThis.String(object.notes) : "",
      type: isSet(object.type) ? complianceTypeFromJSON(object.type) : 0,
      referenceNumber: isSet(object.referenceNumber)
        ? globalThis.String(object.referenceNumber)
        : isSet(object.reference_number)
        ? globalThis.String(object.reference_number)
        : "",
      complianceAuthority: isSet(object.complianceAuthority)
        ? globalThis.String(object.complianceAuthority)
        : isSet(object.compliance_authority)
        ? globalThis.String(object.compliance_authority)
        : "",
      complianceDetails: isSet(object.complianceDetails)
        ? globalThis.String(object.complianceDetails)
        : isSet(object.compliance_details)
        ? globalThis.String(object.compliance_details)
        : "",
      isOneTime: isSet(object.isOneTime)
        ? globalThis.Boolean(object.isOneTime)
        : isSet(object.is_one_time)
        ? globalThis.Boolean(object.is_one_time)
        : false,
      attachments: globalThis.Array.isArray(object?.attachments)
        ? object.attachments.map((e: any) => Attachment.fromJSON(e))
        : [],
      forceCompliedStatus: isSet(object.forceCompliedStatus)
        ? ForceCompliedStatus.fromJSON(object.forceCompliedStatus)
        : isSet(object.force_complied_status)
        ? ForceCompliedStatus.fromJSON(object.force_complied_status)
        : undefined,
    };
  },

  toJSON(message: MaintenanceTask): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.component !== 0) {
      obj.component = componentTypeToJSON(message.component);
    }
    if (message.rules?.length) {
      obj.rules = message.rules.map((e) => InspectionRule.toJSON(e));
    }
    if (message.forceDueDate !== undefined) {
      obj.forceDueDate = message.forceDueDate.toISOString();
    }
    if (message.forceDueEngineHour !== 0) {
      obj.forceDueEngineHour = message.forceDueEngineHour;
    }
    if (message.notes !== "") {
      obj.notes = message.notes;
    }
    if (message.type !== 0) {
      obj.type = complianceTypeToJSON(message.type);
    }
    if (message.referenceNumber !== "") {
      obj.referenceNumber = message.referenceNumber;
    }
    if (message.complianceAuthority !== "") {
      obj.complianceAuthority = message.complianceAuthority;
    }
    if (message.complianceDetails !== "") {
      obj.complianceDetails = message.complianceDetails;
    }
    if (message.isOneTime !== false) {
      obj.isOneTime = message.isOneTime;
    }
    if (message.attachments?.length) {
      obj.attachments = message.attachments.map((e) => Attachment.toJSON(e));
    }
    if (message.forceCompliedStatus !== undefined) {
      obj.forceCompliedStatus = ForceCompliedStatus.toJSON(message.forceCompliedStatus);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MaintenanceTask>, I>>(base?: I): MaintenanceTask {
    return MaintenanceTask.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MaintenanceTask>, I>>(object: I): MaintenanceTask {
    const message = createBaseMaintenanceTask();
    message.id = object.id ?? "";
    message.title = object.title ?? "";
    message.component = object.component ?? 0;
    message.rules = object.rules?.map((e) => InspectionRule.fromPartial(e)) || [];
    message.forceDueDate = object.forceDueDate ?? undefined;
    message.forceDueEngineHour = object.forceDueEngineHour ?? 0;
    message.notes = object.notes ?? "";
    message.type = object.type ?? 0;
    message.referenceNumber = object.referenceNumber ?? "";
    message.complianceAuthority = object.complianceAuthority ?? "";
    message.complianceDetails = object.complianceDetails ?? "";
    message.isOneTime = object.isOneTime ?? false;
    message.attachments = object.attachments?.map((e) => Attachment.fromPartial(e)) || [];
    message.forceCompliedStatus = (object.forceCompliedStatus !== undefined && object.forceCompliedStatus !== null)
      ? ForceCompliedStatus.fromPartial(object.forceCompliedStatus)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
