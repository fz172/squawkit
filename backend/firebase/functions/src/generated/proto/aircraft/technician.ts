// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: aircraft/technician.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "";

export enum CertificateType {
  CERTIFICATE_TYPE_NONE = 0,
  CERTIFICATE_TYPE_REPAIRMAN = 1,
  CERTIFICATE_TYPE_AMT = 2,
  UNRECOGNIZED = -1,
}

export function certificateTypeFromJSON(object: any): CertificateType {
  switch (object) {
    case 0:
    case "CERTIFICATE_TYPE_NONE":
      return CertificateType.CERTIFICATE_TYPE_NONE;
    case 1:
    case "CERTIFICATE_TYPE_REPAIRMAN":
      return CertificateType.CERTIFICATE_TYPE_REPAIRMAN;
    case 2:
    case "CERTIFICATE_TYPE_AMT":
      return CertificateType.CERTIFICATE_TYPE_AMT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CertificateType.UNRECOGNIZED;
  }
}

export function certificateTypeToJSON(object: CertificateType): string {
  switch (object) {
    case CertificateType.CERTIFICATE_TYPE_NONE:
      return "CERTIFICATE_TYPE_NONE";
    case CertificateType.CERTIFICATE_TYPE_REPAIRMAN:
      return "CERTIFICATE_TYPE_REPAIRMAN";
    case CertificateType.CERTIFICATE_TYPE_AMT:
      return "CERTIFICATE_TYPE_AMT";
    case CertificateType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum CertExpireLimit {
  CERT_EXPIRE_LIMIT_UNKNOWN = 0,
  CERT_EXPIRE_LIMIT_EXPIRES = 1,
  CERT_EXPIRE_LIMIT_NEVER_EXPIRES = 2,
  UNRECOGNIZED = -1,
}

export function certExpireLimitFromJSON(object: any): CertExpireLimit {
  switch (object) {
    case 0:
    case "CERT_EXPIRE_LIMIT_UNKNOWN":
      return CertExpireLimit.CERT_EXPIRE_LIMIT_UNKNOWN;
    case 1:
    case "CERT_EXPIRE_LIMIT_EXPIRES":
      return CertExpireLimit.CERT_EXPIRE_LIMIT_EXPIRES;
    case 2:
    case "CERT_EXPIRE_LIMIT_NEVER_EXPIRES":
      return CertExpireLimit.CERT_EXPIRE_LIMIT_NEVER_EXPIRES;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CertExpireLimit.UNRECOGNIZED;
  }
}

export function certExpireLimitToJSON(object: CertExpireLimit): string {
  switch (object) {
    case CertExpireLimit.CERT_EXPIRE_LIMIT_UNKNOWN:
      return "CERT_EXPIRE_LIMIT_UNKNOWN";
    case CertExpireLimit.CERT_EXPIRE_LIMIT_EXPIRES:
      return "CERT_EXPIRE_LIMIT_EXPIRES";
    case CertExpireLimit.CERT_EXPIRE_LIMIT_NEVER_EXPIRES:
      return "CERT_EXPIRE_LIMIT_NEVER_EXPIRES";
    case CertExpireLimit.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Technician {
  id: string;
  name: string;
  /** legacy string field — prefer certificate_type (field 7) */
  certType: string;
  certNumber: string;
  certExpiration: Date | undefined;
  certExpireLimit: CertExpireLimit;
  certificateType: CertificateType;
  /**
   * Owning uid of the first-party profile this was snapshotted from — the member's own
   * self-record, or another share member's published mirror. Empty for a manual entry typed by
   * hand. Drives the provenance label and the self-signed check (design §7.3/§7.5).
   */
  sourceUid: string;
}

function createBaseTechnician(): Technician {
  return {
    id: "",
    name: "",
    certType: "",
    certNumber: "",
    certExpiration: undefined,
    certExpireLimit: 0,
    certificateType: 0,
    sourceUid: "",
  };
}

export const Technician: MessageFns<Technician> = {
  encode(message: Technician, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.certType !== "") {
      writer.uint32(26).string(message.certType);
    }
    if (message.certNumber !== "") {
      writer.uint32(34).string(message.certNumber);
    }
    if (message.certExpiration !== undefined) {
      Timestamp.encode(toTimestamp(message.certExpiration), writer.uint32(42).fork()).join();
    }
    if (message.certExpireLimit !== 0) {
      writer.uint32(48).int32(message.certExpireLimit);
    }
    if (message.certificateType !== 0) {
      writer.uint32(56).int32(message.certificateType);
    }
    if (message.sourceUid !== "") {
      writer.uint32(66).string(message.sourceUid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Technician {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTechnician();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.certType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.certNumber = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.certExpiration = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.certExpireLimit = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.certificateType = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.sourceUid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Technician {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      certType: isSet(object.certType)
        ? globalThis.String(object.certType)
        : isSet(object.cert_type)
        ? globalThis.String(object.cert_type)
        : "",
      certNumber: isSet(object.certNumber)
        ? globalThis.String(object.certNumber)
        : isSet(object.cert_number)
        ? globalThis.String(object.cert_number)
        : "",
      certExpiration: isSet(object.certExpiration)
        ? fromJsonTimestamp(object.certExpiration)
        : isSet(object.cert_expiration)
        ? fromJsonTimestamp(object.cert_expiration)
        : undefined,
      certExpireLimit: isSet(object.certExpireLimit)
        ? certExpireLimitFromJSON(object.certExpireLimit)
        : isSet(object.cert_expire_limit)
        ? certExpireLimitFromJSON(object.cert_expire_limit)
        : 0,
      certificateType: isSet(object.certificateType)
        ? certificateTypeFromJSON(object.certificateType)
        : isSet(object.certificate_type)
        ? certificateTypeFromJSON(object.certificate_type)
        : 0,
      sourceUid: isSet(object.sourceUid)
        ? globalThis.String(object.sourceUid)
        : isSet(object.source_uid)
        ? globalThis.String(object.source_uid)
        : "",
    };
  },

  toJSON(message: Technician): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.certType !== "") {
      obj.certType = message.certType;
    }
    if (message.certNumber !== "") {
      obj.certNumber = message.certNumber;
    }
    if (message.certExpiration !== undefined) {
      obj.certExpiration = message.certExpiration.toISOString();
    }
    if (message.certExpireLimit !== 0) {
      obj.certExpireLimit = certExpireLimitToJSON(message.certExpireLimit);
    }
    if (message.certificateType !== 0) {
      obj.certificateType = certificateTypeToJSON(message.certificateType);
    }
    if (message.sourceUid !== "") {
      obj.sourceUid = message.sourceUid;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Technician>, I>>(base?: I): Technician {
    return Technician.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Technician>, I>>(object: I): Technician {
    const message = createBaseTechnician();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.certType = object.certType ?? "";
    message.certNumber = object.certNumber ?? "";
    message.certExpiration = object.certExpiration ?? undefined;
    message.certExpireLimit = object.certExpireLimit ?? 0;
    message.certificateType = object.certificateType ?? 0;
    message.sourceUid = object.sourceUid ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
