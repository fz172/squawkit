// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: sharing/shared_aircraft_ref.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

export enum ShareRole {
  SHARE_ROLE_UNKNOWN = 0,
  SHARE_ROLE_OWNER = 1,
  SHARE_ROLE_TECHNICIAN = 2,
  UNRECOGNIZED = -1,
}

export function shareRoleFromJSON(object: any): ShareRole {
  switch (object) {
    case 0:
    case "SHARE_ROLE_UNKNOWN":
      return ShareRole.SHARE_ROLE_UNKNOWN;
    case 1:
    case "SHARE_ROLE_OWNER":
      return ShareRole.SHARE_ROLE_OWNER;
    case 2:
    case "SHARE_ROLE_TECHNICIAN":
      return ShareRole.SHARE_ROLE_TECHNICIAN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ShareRole.UNRECOGNIZED;
  }
}

export function shareRoleToJSON(object: ShareRole): string {
  switch (object) {
    case ShareRole.SHARE_ROLE_UNKNOWN:
      return "SHARE_ROLE_UNKNOWN";
    case ShareRole.SHARE_ROLE_OWNER:
      return "SHARE_ROLE_OWNER";
    case ShareRole.SHARE_ROLE_TECHNICIAN:
      return "SHARE_ROLE_TECHNICIAN";
    case ShareRole.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * A member-side pointer to a shared aircraft that physically lives under another
 * account's tree. Stored as a synced entity at users/{memberUid}/shared_aircraft_ref/{aircraftId}
 * so it rides the existing hydrate/pull/push machinery and is available offline.
 * Written by the sharing Cloud Functions, not the client. See docs/sharing §2.2.
 */
export interface SharedAircraftRef {
  aircraftId: string;
  hostUid: string;
  /**
   * Mirror of the ACL role — a UI hint only. Enforcement is the aircraft_shares ACL;
   * security rules never consult this ref.
   */
  role: ShareRole;
}

function createBaseSharedAircraftRef(): SharedAircraftRef {
  return { aircraftId: "", hostUid: "", role: 0 };
}

export const SharedAircraftRef: MessageFns<SharedAircraftRef> = {
  encode(message: SharedAircraftRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.aircraftId !== "") {
      writer.uint32(10).string(message.aircraftId);
    }
    if (message.hostUid !== "") {
      writer.uint32(18).string(message.hostUid);
    }
    if (message.role !== 0) {
      writer.uint32(24).int32(message.role);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SharedAircraftRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSharedAircraftRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.aircraftId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hostUid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SharedAircraftRef {
    return {
      aircraftId: isSet(object.aircraftId)
        ? globalThis.String(object.aircraftId)
        : isSet(object.aircraft_id)
        ? globalThis.String(object.aircraft_id)
        : "",
      hostUid: isSet(object.hostUid)
        ? globalThis.String(object.hostUid)
        : isSet(object.host_uid)
        ? globalThis.String(object.host_uid)
        : "",
      role: isSet(object.role) ? shareRoleFromJSON(object.role) : 0,
    };
  },

  toJSON(message: SharedAircraftRef): unknown {
    const obj: any = {};
    if (message.aircraftId !== "") {
      obj.aircraftId = message.aircraftId;
    }
    if (message.hostUid !== "") {
      obj.hostUid = message.hostUid;
    }
    if (message.role !== 0) {
      obj.role = shareRoleToJSON(message.role);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SharedAircraftRef>, I>>(base?: I): SharedAircraftRef {
    return SharedAircraftRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SharedAircraftRef>, I>>(object: I): SharedAircraftRef {
    const message = createBaseSharedAircraftRef();
    message.aircraftId = object.aircraftId ?? "";
    message.hostUid = object.hostUid ?? "";
    message.role = object.role ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
