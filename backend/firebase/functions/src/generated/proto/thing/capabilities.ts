// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: thing/capabilities.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { SquawkPriority, squawkPriorityFromJSON, squawkPriorityToJSON } from "./squawk";

export const protobufPackage = "";

/**
 * The per-Thing sections of the adaptive shell, in the order a template wants them. Mirrors
 * ShellSection's first four values; SETTINGS is account-level and never template-controlled.
 */
export enum Section {
  SECTION_UNKNOWN = 0,
  SECTION_DASHBOARD = 1,
  /** SECTION_SQUAWKS - named by Lexicon.squawk at render — "Issues", "Attention", ... */
  SECTION_SQUAWKS = 2,
  SECTION_TASKS = 3,
  SECTION_LOGS = 4,
  UNRECOGNIZED = -1,
}

export function sectionFromJSON(object: any): Section {
  switch (object) {
    case 0:
    case "SECTION_UNKNOWN":
      return Section.SECTION_UNKNOWN;
    case 1:
    case "SECTION_DASHBOARD":
      return Section.SECTION_DASHBOARD;
    case 2:
    case "SECTION_SQUAWKS":
      return Section.SECTION_SQUAWKS;
    case 3:
    case "SECTION_TASKS":
      return Section.SECTION_TASKS;
    case 4:
    case "SECTION_LOGS":
      return Section.SECTION_LOGS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Section.UNRECOGNIZED;
  }
}

export function sectionToJSON(object: Section): string {
  switch (object) {
    case Section.SECTION_UNKNOWN:
      return "SECTION_UNKNOWN";
    case Section.SECTION_DASHBOARD:
      return "SECTION_DASHBOARD";
    case Section.SECTION_SQUAWKS:
      return "SECTION_SQUAWKS";
    case Section.SECTION_TASKS:
      return "SECTION_TASKS";
    case Section.SECTION_LOGS:
      return "SECTION_LOGS";
    case Section.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ExportLayout {
  EXPORT_LAYOUT_UNKNOWN = 0,
  /** EXPORT_LAYOUT_LOGBOOK - The paper-logbook tab layout the app ships today. Airplane only. */
  EXPORT_LAYOUT_LOGBOOK = 1,
  /** EXPORT_LAYOUT_GENERIC - Columns derived from the lexicon and the template's meter set. */
  EXPORT_LAYOUT_GENERIC = 2,
  UNRECOGNIZED = -1,
}

export function exportLayoutFromJSON(object: any): ExportLayout {
  switch (object) {
    case 0:
    case "EXPORT_LAYOUT_UNKNOWN":
      return ExportLayout.EXPORT_LAYOUT_UNKNOWN;
    case 1:
    case "EXPORT_LAYOUT_LOGBOOK":
      return ExportLayout.EXPORT_LAYOUT_LOGBOOK;
    case 2:
    case "EXPORT_LAYOUT_GENERIC":
      return ExportLayout.EXPORT_LAYOUT_GENERIC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ExportLayout.UNRECOGNIZED;
  }
}

export function exportLayoutToJSON(object: ExportLayout): string {
  switch (object) {
    case ExportLayout.EXPORT_LAYOUT_UNKNOWN:
      return "EXPORT_LAYOUT_UNKNOWN";
    case ExportLayout.EXPORT_LAYOUT_LOGBOOK:
      return "EXPORT_LAYOUT_LOGBOOK";
    case ExportLayout.EXPORT_LAYOUT_GENERIC:
      return "EXPORT_LAYOUT_GENERIC";
    case ExportLayout.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Capabilities {
  /** component tree UI, picker, config card */
  components: boolean;
  /** meter fields on logs; METER-based rules */
  meters: boolean;
  /** type chip, reference number, authority */
  compliance: boolean;
  /** technician picker on the log form */
  technicians: boolean;
  /** nag for serials at creation */
  componentSerialPrompt: boolean;
  /**
   * Which priority values the template offers. The SquawkPriority enum keeps every value
   * regardless — it is stored data on existing squawks (#638) — so this narrows what is OFFERED,
   * never what can be read back.
   */
  priorities: SquawkPriority[];
  /**
   * Which sections appear, and in what order. An ordered list rather than a bool per section, so
   * the shell reads one list instead of every screen checking a flag (design §10).
   */
  sections: Section[];
  exportLayout: ExportLayout;
}

function createBaseCapabilities(): Capabilities {
  return {
    components: false,
    meters: false,
    compliance: false,
    technicians: false,
    componentSerialPrompt: false,
    priorities: [],
    sections: [],
    exportLayout: 0,
  };
}

export const Capabilities: MessageFns<Capabilities> = {
  encode(message: Capabilities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.components !== false) {
      writer.uint32(8).bool(message.components);
    }
    if (message.meters !== false) {
      writer.uint32(16).bool(message.meters);
    }
    if (message.compliance !== false) {
      writer.uint32(24).bool(message.compliance);
    }
    if (message.technicians !== false) {
      writer.uint32(32).bool(message.technicians);
    }
    if (message.componentSerialPrompt !== false) {
      writer.uint32(48).bool(message.componentSerialPrompt);
    }
    writer.uint32(58).fork();
    for (const v of message.priorities) {
      writer.int32(v);
    }
    writer.join();
    writer.uint32(66).fork();
    for (const v of message.sections) {
      writer.int32(v);
    }
    writer.join();
    if (message.exportLayout !== 0) {
      writer.uint32(72).int32(message.exportLayout);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Capabilities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.components = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.meters = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.compliance = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.technicians = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.componentSerialPrompt = reader.bool();
          continue;
        }
        case 7: {
          if (tag === 56) {
            message.priorities.push(reader.int32() as any);

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.priorities.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 8: {
          if (tag === 64) {
            message.sections.push(reader.int32() as any);

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.sections.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.exportLayout = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Capabilities {
    return {
      components: isSet(object.components) ? globalThis.Boolean(object.components) : false,
      meters: isSet(object.meters) ? globalThis.Boolean(object.meters) : false,
      compliance: isSet(object.compliance) ? globalThis.Boolean(object.compliance) : false,
      technicians: isSet(object.technicians) ? globalThis.Boolean(object.technicians) : false,
      componentSerialPrompt: isSet(object.componentSerialPrompt)
        ? globalThis.Boolean(object.componentSerialPrompt)
        : isSet(object.component_serial_prompt)
        ? globalThis.Boolean(object.component_serial_prompt)
        : false,
      priorities: globalThis.Array.isArray(object?.priorities)
        ? object.priorities.map((e: any) => squawkPriorityFromJSON(e))
        : [],
      sections: globalThis.Array.isArray(object?.sections) ? object.sections.map((e: any) => sectionFromJSON(e)) : [],
      exportLayout: isSet(object.exportLayout)
        ? exportLayoutFromJSON(object.exportLayout)
        : isSet(object.export_layout)
        ? exportLayoutFromJSON(object.export_layout)
        : 0,
    };
  },

  toJSON(message: Capabilities): unknown {
    const obj: any = {};
    if (message.components !== false) {
      obj.components = message.components;
    }
    if (message.meters !== false) {
      obj.meters = message.meters;
    }
    if (message.compliance !== false) {
      obj.compliance = message.compliance;
    }
    if (message.technicians !== false) {
      obj.technicians = message.technicians;
    }
    if (message.componentSerialPrompt !== false) {
      obj.componentSerialPrompt = message.componentSerialPrompt;
    }
    if (message.priorities?.length) {
      obj.priorities = message.priorities.map((e) => squawkPriorityToJSON(e));
    }
    if (message.sections?.length) {
      obj.sections = message.sections.map((e) => sectionToJSON(e));
    }
    if (message.exportLayout !== 0) {
      obj.exportLayout = exportLayoutToJSON(message.exportLayout);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Capabilities>, I>>(base?: I): Capabilities {
    return Capabilities.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Capabilities>, I>>(object: I): Capabilities {
    const message = createBaseCapabilities();
    message.components = object.components ?? false;
    message.meters = object.meters ?? false;
    message.compliance = object.compliance ?? false;
    message.technicians = object.technicians ?? false;
    message.componentSerialPrompt = object.componentSerialPrompt ?? false;
    message.priorities = object.priorities?.map((e) => e) || [];
    message.sections = object.sections?.map((e) => e) || [];
    message.exportLayout = object.exportLayout ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
