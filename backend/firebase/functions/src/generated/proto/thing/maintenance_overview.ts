// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: thing/maintenance_overview.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { MeterReading } from "./meter_reading";

export const protobufPackage = "";

/**
 * Summary data for an aircraft's maintenance history.
 * Used to avoid recomputing statistics from the full log list.
 */
export interface MaintenanceOverview {
  aircraftId: string;
  totalLogCount: number;
  /** Counts by component type */
  airframeLogCount: number;
  engineLogCount: number;
  propellerLogCount: number;
  /**
   * Current accumulated times. Aviation-only by construction and superseded by `current` below.
   *
   * The current reading for every meter this Thing's logs carry: the maximum per key (PRD §4.4).
   *
   * Replaced `current_airframe_time` (6), `current_engine_time` (7) and `current_propeller_time`
   * (8). A car's odometer had nowhere to go in three aviation doubles, which is why the dashboard
   * could ask for one and never be answered.
   */
  current: MeterReading[];
}

function createBaseMaintenanceOverview(): MaintenanceOverview {
  return {
    aircraftId: "",
    totalLogCount: 0,
    airframeLogCount: 0,
    engineLogCount: 0,
    propellerLogCount: 0,
    current: [],
  };
}

export const MaintenanceOverview: MessageFns<MaintenanceOverview> = {
  encode(message: MaintenanceOverview, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.aircraftId !== "") {
      writer.uint32(10).string(message.aircraftId);
    }
    if (message.totalLogCount !== 0) {
      writer.uint32(16).uint32(message.totalLogCount);
    }
    if (message.airframeLogCount !== 0) {
      writer.uint32(24).uint32(message.airframeLogCount);
    }
    if (message.engineLogCount !== 0) {
      writer.uint32(32).uint32(message.engineLogCount);
    }
    if (message.propellerLogCount !== 0) {
      writer.uint32(40).uint32(message.propellerLogCount);
    }
    for (const v of message.current) {
      MeterReading.encode(v!, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MaintenanceOverview {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMaintenanceOverview();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.aircraftId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalLogCount = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.airframeLogCount = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.engineLogCount = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.propellerLogCount = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.current.push(MeterReading.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MaintenanceOverview {
    return {
      aircraftId: isSet(object.aircraftId)
        ? globalThis.String(object.aircraftId)
        : isSet(object.aircraft_id)
        ? globalThis.String(object.aircraft_id)
        : "",
      totalLogCount: isSet(object.totalLogCount)
        ? globalThis.Number(object.totalLogCount)
        : isSet(object.total_log_count)
        ? globalThis.Number(object.total_log_count)
        : 0,
      airframeLogCount: isSet(object.airframeLogCount)
        ? globalThis.Number(object.airframeLogCount)
        : isSet(object.airframe_log_count)
        ? globalThis.Number(object.airframe_log_count)
        : 0,
      engineLogCount: isSet(object.engineLogCount)
        ? globalThis.Number(object.engineLogCount)
        : isSet(object.engine_log_count)
        ? globalThis.Number(object.engine_log_count)
        : 0,
      propellerLogCount: isSet(object.propellerLogCount)
        ? globalThis.Number(object.propellerLogCount)
        : isSet(object.propeller_log_count)
        ? globalThis.Number(object.propeller_log_count)
        : 0,
      current: globalThis.Array.isArray(object?.current)
        ? object.current.map((e: any) => MeterReading.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MaintenanceOverview): unknown {
    const obj: any = {};
    if (message.aircraftId !== "") {
      obj.aircraftId = message.aircraftId;
    }
    if (message.totalLogCount !== 0) {
      obj.totalLogCount = Math.round(message.totalLogCount);
    }
    if (message.airframeLogCount !== 0) {
      obj.airframeLogCount = Math.round(message.airframeLogCount);
    }
    if (message.engineLogCount !== 0) {
      obj.engineLogCount = Math.round(message.engineLogCount);
    }
    if (message.propellerLogCount !== 0) {
      obj.propellerLogCount = Math.round(message.propellerLogCount);
    }
    if (message.current?.length) {
      obj.current = message.current.map((e) => MeterReading.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MaintenanceOverview>, I>>(base?: I): MaintenanceOverview {
    return MaintenanceOverview.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MaintenanceOverview>, I>>(object: I): MaintenanceOverview {
    const message = createBaseMaintenanceOverview();
    message.aircraftId = object.aircraftId ?? "";
    message.totalLogCount = object.totalLogCount ?? 0;
    message.airframeLogCount = object.airframeLogCount ?? 0;
    message.engineLogCount = object.engineLogCount ?? 0;
    message.propellerLogCount = object.propellerLogCount ?? 0;
    message.current = object.current?.map((e) => MeterReading.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
