// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.19.1
// source: thing/meter_reading.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

/**
 * One meter's value, as recorded on a log or accumulated on a Thing. PRD §4.4.
 *
 * Replaces the three `double` hour fields that could only ever describe an aeroplane. A car's
 * odometer, a bike's ride hours and an airframe's tach all land here because the KEY carries the
 * meaning — which is what makes "every 5,000 miles" and "every 100 hours" the same feature.
 */
export interface MeterReading {
  /**
   * Matches MeterDef.key on the template. A key no template declares is kept, never dropped: it is
   * the user's reading, and a template can stop declaring a meter while its history remains.
   */
  meterKey: string;
  /**
   * Which component this reading belongs to, empty for a Thing-level meter. An engine's hours are
   * that engine's, and a twin records two of them; airframe time belongs to the Thing.
   *
   * The stable `Component.id`, not a serial: a blank serial is universal on bikes and common on
   * home fixtures, and replacing a part with a new serial silently re-points its history (§4.3).
   */
  componentId: string;
  value: number;
}

function createBaseMeterReading(): MeterReading {
  return { meterKey: "", componentId: "", value: 0 };
}

export const MeterReading: MessageFns<MeterReading> = {
  encode(message: MeterReading, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.meterKey !== "") {
      writer.uint32(10).string(message.meterKey);
    }
    if (message.componentId !== "") {
      writer.uint32(18).string(message.componentId);
    }
    if (message.value !== 0) {
      writer.uint32(25).double(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MeterReading {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMeterReading();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.meterKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.componentId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.value = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MeterReading {
    return {
      meterKey: isSet(object.meterKey)
        ? globalThis.String(object.meterKey)
        : isSet(object.meter_key)
        ? globalThis.String(object.meter_key)
        : "",
      componentId: isSet(object.componentId)
        ? globalThis.String(object.componentId)
        : isSet(object.component_id)
        ? globalThis.String(object.component_id)
        : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: MeterReading): unknown {
    const obj: any = {};
    if (message.meterKey !== "") {
      obj.meterKey = message.meterKey;
    }
    if (message.componentId !== "") {
      obj.componentId = message.componentId;
    }
    if (message.value !== 0) {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MeterReading>, I>>(base?: I): MeterReading {
    return MeterReading.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MeterReading>, I>>(object: I): MeterReading {
    const message = createBaseMeterReading();
    message.meterKey = object.meterKey ?? "";
    message.componentId = object.componentId ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
