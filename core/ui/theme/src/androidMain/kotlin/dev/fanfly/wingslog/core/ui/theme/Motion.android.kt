package dev.fanfly.wingslog.core.ui.theme

// Compose already applies the system animator duration scale, so "Remove animations" needs no help.
internal actual fun isReduceMotionEnabled(): Boolean = false
