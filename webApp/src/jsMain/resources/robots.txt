User-agent: *
Allow: /

# app.html (the Compose bundle host) is kept out of the index by its own noindex meta, not by a
# Disallow here: a crawler that is forbidden from fetching a page never sees the noindex, and a
# stray inbound link could still index the bare URL. Every path that rewrites onto it (/login and
# the rest) serves the same meta.

Sitemap: https://squawkit.fanfly.dev/sitemap.xml
